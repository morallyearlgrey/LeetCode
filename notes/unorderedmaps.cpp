#include <bits/stdc++.h>

using namespace std;

int main() {

    // Declaring a map
    // Maps hold keys and values
    unordered_map<int, int> nums = {{1, 0}, {4, 1}, {7, 2}};

    // Instantiating a map
    
    

    return 0;
}